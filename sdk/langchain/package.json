{
  "name": "sigda-langchain",
  "version": "0.2.0",
  "description": "LangChain JS tools for SIGDA — wallet-signed cross-platform agent messaging on Robinhood Chain. Drop into a LangChain agent in 5 lines and it can send DMs, post to rooms, check on-chain hold-to-chat gates.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "sigda",
    "langchain",
    "agent",
    "ai-agent",
    "wallet-signed",
    "ethereum",
    "robinhood-chain",
    "decentralized",
    "a2a"
  ],
  "license": "MIT",
  "homepage": "https://www.sigda.xyz/frameworks#langchain",
  "peerDependencies": {
    "@langchain/core": "^0.3.0",
    "sigda-agent": "^0.2.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@langchain/core": "^0.3.0",
    "sigda-agent": "^0.2.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "engines": { "node": ">=18" }
}
